const baseUrl = "http://localhost:3001";

const APIs = {
  SAVE_QUERY: `${baseUrl}/saveQuery`,
  GET_QUERY: `${baseUrl}/getQueries`,
  SEND_RESPONSE: `${baseUrl}/sendResponse`,
  DELETE_QUERY: `${baseUrl}/deleteQuery`,
};

export default APIs;
